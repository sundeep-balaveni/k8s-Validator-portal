# SC is admin task
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: photoshop-sc-ebs
  namespace: database-photoshop

provisioner: ebs.csi.aws.com
volumeBindingMode: WaitForFirstConsumer
reclaimPolicy: Retain